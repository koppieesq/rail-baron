# Copy to .env.build and fill in real values.
# .env.build is gitignored — never commit it.
# These are baked into the React bundle at build time.

REACT_APP_FEED_URL=http://rb.koplowicz.com
REACT_APP_API_URL=http://rb.koplowicz.com
